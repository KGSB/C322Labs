Log 1:

Created the assignment, it reads some of the given input incorrectly.
The printing format is a bit off.

Log 2:

Figured out that the leftmost bit is irrelevant and is the reason for input being read incorrectly.

Log 3:

Had to change to low level file accessing. Accounting for nonprintable ASCII characters.
